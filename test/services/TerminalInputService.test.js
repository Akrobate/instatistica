'use strict';

const {
    TerminalInputService,
} = require('../../src/services/TerminalInputService');

describe('TerminalInputService', () => {

    it('Should be able to parse input', () => {
        const terminal_input_service = TerminalInputService.getInstance();
    });
});
