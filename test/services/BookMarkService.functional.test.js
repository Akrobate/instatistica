'use strict';

const {
    BookMarkService,
} = require('../../services');

const {
    mock,
    stub,
} = require('sinon');


describe('test', () => {

    beforeEach(() => {

    });

    afterEach(() => {

    });

    it('Should be able to save file with tags', (done) => {
        done();
    });

});
