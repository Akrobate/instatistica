'use strict';

const {
    JsonFileRepository,
} = require('./JsonFileRepository');

const {
    FileRepository,
} = require('./FileRepository');



module.exports = {
    JsonFileRepository,
    FileRepository,
};
