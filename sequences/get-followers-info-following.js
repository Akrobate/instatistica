/**
 * This script get all user follower list
 * with parameter to know if user if user
 * is following back or not
 */

'use strict';

const auth = require('../auth');

const follower_info_file = '';


(async () => {
})();
